import type { PropertyFilterProps } from '@awsui/components-react/property-filter';

const COMPARISON_OPERATORS: PropertyFilterProps.ComparisonOperator[] = [
  '>=',
  '<=',
  '!:',
  '!=',
  ':',
  '>',
  '<',
  '=',
];

export default COMPARISON_OPERATORS;
